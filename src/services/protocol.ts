/**
 * Client protocol types — re-exported from @connectx/shared.
 */
export type {
  PlayerInfo,
  MatchConfig,
  RoomConfig,
  ClientToServerEvents,
  ServerToClientEvents,
} from '@connectx/shared/protocol';
