/**
 * Board operations — re-exported from @connectx/shared.
 */
export {
  createBoard,
  cloneBoard,
  dropPiece,
  isValidMove,
  getValidMoves,
  isBoardFull,
  createBlockedGrid,
  generateBlockedCells,
} from '@connectx/shared/engine';