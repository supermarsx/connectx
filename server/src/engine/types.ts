/**
 * Server engine types — re-exported from @connectx/shared for backward compatibility.
 */
export {
  EMPTY_CELL,
  DEFAULT_BOARD_CONFIG,
  PLAYER_COLORS,
} from '@connectx/shared/engine';

export type {
  Board,
  Cell,
  PlayerId,
  GameMode,
  BotDifficulty,
  BoardConfig,
} from '@connectx/shared/engine';